# 🤖 YOLOv8 手部识别模型

> 基于 YOLOv8 框架的手部检测解决方案，提供从数据处理、模型训练到推理部署的完整流程

[![Python](https://img.shields.io/badge/Python-3.11-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-Apache%202.0-green.svg)](LICENSE)
[![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20Linux%20%7C%20macOS-lightgrey.svg)]()

## ✨ 项目特色

- 🎯 **端到端流程**：从数据集转换到模型训练，一键化操作
- 📊 **数据集管理**：支持 COCO 格式自动转换为 YOLO 训练格式
- 🚀 **高效训练**：基于预训练模型快速微调，支持断点续训
- 📈 **性能可视化**：训练过程全监控，生成精确率/召回率/F1曲线
- 🖼️ **即用推理**：训练完成即可使用最佳权重进行手部检测

## 🚀 快速开始

### 安装依赖

```bash
#推荐创建虚拟环境
pip install -r requirements.txt
```

### 一键推理

```bash
# 调用摄像头进行手部检测
python ModleTestCamera.py
```



## 模型训练

## 📦 数据准备

### 数据集来源

本项目使用魔塔社区的手部检测数据集：

- **数据集网址**：`https://www.modelscope.cn/datasets/modelscope/hand_detection_dataset/files`
- **Git 克隆**：`git clone https://www.modelscope.cn/datasets/modelscope/hand_detection_dataset.git`

### 数据集文件(手动下载)

1. 访问上方网址下载压缩包
2. 解压到项目根目录，确保结构如下：

```
hand_detection_dataset/
├── annotations/          # 标注文件
├── train2017/           # 训练图片 (25,933张)
├── val2017/             # 验证图片 (6,484张)
├── Readme.md
└── hand_detection_dataset.json
```

### 格式转换

数据格式为COCO，需转换为 YOLO 格式：

```bash
python convert_coco_to_yolo.py
```

**转换后结构**：
```
hand_detection_dataset_converted/
├── train/
│   ├── images/          # 训练图片
│   └── labels/          # YOLO格式标签
└── validation/
    └── labels/          # 验证标签
```

## 🔧 核心命令

### 1. 完整训练流程

```bash
# 自动化训练
python train_hand_detector.py
```

**流程说明**：
- 自动检查数据集是否存在
- 自动转换格式（如需）
- 使用 `yolo11n.pt` 预训练模型启动训练
- 结果保存在 `runs/detect/`

### 2. 模型推理

训练完成后，使用最佳权重进行推理：

```python
# 使用训练好的模型检测图片
from ultralytics import YOLO
model = YOLO('runs/detect/yolo11n_hand_detect.pt2/weights/best.pt')
results = model('input.jpg')
results.show()
```

### 3.摄像头检测测试

调用笔记本摄像头进行检测

```
# 离摄像头 50 cm及以外，效果最佳
python ModleTestCamera.py
```

### 4.本地图像检测测试

读取本地路径图像/目录(批量

```bash
python ModleTestPhoto.py
```





## 📂 项目结构详解

```
YOLOv8/
├── hand_detection_dataset/              # 原始数据集（需下载）
│   ├── annotations/                     # COCO标注文件
│   ├── train2017/                       # 训练图片
│   └── val2017/                         # 验证图片

├── hand_detection_dataset_converted/    # 转换后数据集（自动生成）
│   ├── train/
│   │   ├── images/                      # 训练图片
│   │   └── labels/                      # YOLO标签
│   └── validation/
│       └── labels/                      # 验证标签

├── runs/detect/                         # 训练结果（自动生成）
│   └── yolo11n_hand_detect.pt2/        # 单次训练结果
│       ├── weights/
│       │   ├── best.pt                  # 最佳模型权重 ⭐
│       │   └── last.pt                  # 最终模型权重
│       ├── val_batch*.jpg               # 验证可视化
│       └── results.png                  # 训练指标曲线

├── hand_detection_dataset.yaml          # YOLO数据集配置
├── train_hand_detector.py               # 训练主脚本
├── convert_coco_to_yolo.py              # 格式转换脚本
├── ModleTestCamera.py					 # 调用摄像头
├── ModleTestPhoto.py					 # 图片推理(可批量)
├── ModleUrlCameraTest.py				 # 网页调用摄像头(未优化) 
├── yolo11n.pt                           # YOLOv8预训练模型
├── requirements.txt                     # 依赖清单
└── README.md                            # 本文档
```

## 📊 训练结果解读

训练完成后，`runs/detect/` 目录包含：

| 文件/目录              | 说明                             |
| ---------------------- | -------------------------------- |
| `weights/best.pt`      | **最佳模型权重**（推荐用于推理） |
| `weights/last.pt`      | 最终训练的权重                   |
| `results.png`          | 训练指标曲线（精度/召回/F1）     |
| `val_batch*.jpg`       | 验证集预测可视化                 |
| `confusion_matrix.png` | 混淆矩阵分析                     |

## 📋 重要文件说明

| 文件名                        | 作用               | 是否必须   |
| ----------------------------- | ------------------ | ---------- |
| `hand_detection_dataset.yaml` | YOLO数据集配置文件 | ✅ 自动生成 |
| `yolo11n.pt`                  | 预训练模型起点     | ✅ 不可删除 |
| `train_hand_detector.py`      | 训练脚本           | ✅ 核心文件 |
| `convert_coco_to_yolo.py`     | 格式转换脚本       | ✅ 工具文件 |
| `项目详细结构.md`             | 项目详细结构文档   | ℹ️ 参考文件 |



## 💡 使用示例

### 场景1：首次完整训练

```bash
# 1. 下载数据集并解压
# 2. 开始训练
python train_hand_detector.py

# 3. 查看训练结果
cd runs/detect/yolo11n_hand_detect.pt2/
# 查看 results.png 了解训练效果
```

### 场景2：仅转换数据格式

```bash
# 如果已有原始数据集，只想转换格式
python convert_coco_to_yolo.py
```

### 场景3：加载模型进行推理

```bash
# 使用训练好的模型检测图片
python ModleTestPhoto.py(更改路径可以批量检测)
```



## 🐛 故障排除

### 常见问题

**Q: 运行训练脚本时找不到数据集？**
A: 确保 `hand_detection_dataset/` 文件夹已正确解压到项目根目录，包含 `annotations/`、`train2017/`、`val2017/`

**Q: 转换脚本报错？**
A: 检查 `hand_detection_dataset.json` 文件是否存在且格式正确

**Q: 训练速度很慢？**
A: 建议使用GPU训练。检查CUDA是否可用：
```python
import torch
print(torch.cuda.is_available())  # 应返回 True
```

**Q: 如何恢复训练？**
A: 修改 `train_hand_detector.py` 中的 `resume=True` 参数，或手动指定 `weights=last.pt`

### 获取帮助

```bash
# 查看YOLOv8训练参数帮助
yolo train --help

# 查看数据集配置
cat hand_detection_dataset.yaml
```

## 📊 性能指标

训练完成后，您将获得：

- **精确率(Precision)**：检测框的准确性
- **召回率(Recall)**：漏检率控制
- **mAP@0.5**: 0.5 IoU阈值下的平均精度
- **mAP@0.5:0.95**: 多IoU阈值下的综合精度

## 📄 许可证

本项目采用 **Apache License 2.0** - 详见 [LICENSE](LICENSE) 文件。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request 来改进项目！

## 📞 联系方式

- 项目主页: [K3-GPT/PDF2](https://github.com/K3-GPT/PDF2)
- 邮箱: verus_amor@qq.com
